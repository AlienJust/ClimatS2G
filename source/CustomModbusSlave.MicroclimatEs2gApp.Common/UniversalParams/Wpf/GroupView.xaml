<UserControl x:Class="CustomModbusSlave.MicroclimatEs2gApp.Common.UniversalParams.Wpf.GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomModbusSlave.MicroclimatEs2gApp.Common.UniversalParams.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<local:GroupParamTemplateSelector x:Key="GroupParamTemplateSelector"/>
	</UserControl.Resources>
	<StackPanel Orientation="Vertical">
		<TextBlock Text="{Binding Path=Name, Mode=OneTime}"/>
		<ListBox ItemsSource="{Binding Path=Children, Mode=OneTime}" ItemTemplateSelector="{StaticResource GroupParamTemplateSelector}">
		</ListBox>
	</StackPanel>
</UserControl>
