<UserControl x:Class="CustomModbusSlave.Es2gClimatic.Shared.MukFlap.Views.MukFlapDiagnosticOneWireSensorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views1="clr-namespace:CustomModbusSlave.Es2gClimatic.Shared.MukFlap.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel>
		<CheckBox Content="Ошибка датчика" IsChecked="{Binding Path=OneWireSensorError, Mode=OneTime}"/>
		<views1:OneWireErrorCodeView DataContext="{Binding Path=ErrorCode, Mode=OneTime}"/>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			<TextBlock Text="Число ошибок связи"/>
			<TextBlock Grid.Column="1" Text="{Binding Path=LinkErrorCount, Mode=OneTime}"/>
		</Grid>
	</StackPanel>
</UserControl>
