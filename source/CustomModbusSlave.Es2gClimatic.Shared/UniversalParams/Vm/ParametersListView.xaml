<UserControl x:Class="ParamControls.Vm.ParametersListView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:ParamControls.Vm"
	xmlns:vm="clr-namespace:CustomModbusSlave.Es2gClimatic.Shared.UniversalParams.Vm"
	mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<DataTemplate x:Key="ChartParameterTemplate">
			<vm:ChartParameterView/>
		</DataTemplate>
		<DataTemplate x:Key="DisplayGroupTemplate">
			<local:ParametersListView />
		</DataTemplate>
		<DataTemplate x:Key="CannotDisplayMessageTemplate">
			<TextBlock Text="Не удалось отобразить элемент" />
		</DataTemplate>

		<local:ParameterListItemTemplateSelector x:Key="ParameterListItemTemplateSelector"></local:ParameterListItemTemplateSelector>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBox Text="{Binding Path=DisplayName, Mode=OneTime}"></TextBox>
		<ListBox ItemTemplateSelector="{StaticResource ParameterListItemTemplateSelector}" ItemsSource="{Binding Path=GroupItems, Mode=OneTime}">

		</ListBox>
	</Grid>
	
</UserControl>
