<UserControl x:Class="ParamControls.Vm.ParametersListView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:ParamControls.Vm" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<DataTemplate x:Key="IntegerParameterDisplayTemplate">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>
				<TextBox Text="{Binding Path=DisplayName, Mode=OneTime}"/>
				<TextBox Text="{Binding Path=DisplayValue, Mode=OneWay}"/>
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="CannotDisplayMessageTemplate">
			<TextBox Text="Не удалось отобразить параметр" />
		</DataTemplate>
	</UserControl.Resources>
	<ListBox>
		<DataTemplateSelector></DataTemplateSelector>
	</ListBox>
</UserControl>
