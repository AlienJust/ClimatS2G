<UserControl x:Class="CustomModbusSlave.Es2gClimatic.InteriorApp.MukFlapOuterAir.Reply03.EmersionDiagnosticCircuit2View"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
		d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel>
		<CheckBox Content="N/A" IsChecked="{Binding Path=NotAvailable, Mode=OneTime}" />
		<CheckBox Content="Низкое давление" IsChecked="{Binding Path=LowPressure, Mode=OneTime}" />
		<CheckBox Content="Высокий перегрев" IsChecked="{Binding Path=HighOverheat, Mode=OneTime}" />
		<CheckBox Content="Низкий перегрев" IsChecked="{Binding Path=LowOverheat, Mode=OneTime}" />
		<CheckBox Content="Замерзание" IsChecked="{Binding Path=Freeze, Mode=OneTime}" />
	</StackPanel>
</UserControl>
